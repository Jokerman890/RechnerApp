# --------------------------------------------------
# Frontend (öffentlich, wird in den Browser gebundled)
# Nur Variablen mit VITE_ Prefix verwenden.
# --------------------------------------------------
VITE_APP_URL="http://localhost:3000"

# --------------------------------------------------
# Backend (geheim, darf nie im Browser landen)
# Diese Variablen werden ausschließlich serverseitig verwendet.
# --------------------------------------------------
GEMINI_API_KEY="MY_GEMINI_API_KEY"
PORT="8787"
